/**
 * BasicInfoForm.tsx
 *
 * NOTE: The full form implementation lives in
 * ../steps/BasicInfoStep.tsx — that component owns all
 * state, lookup loading, and store integration.
 *
 * This file is kept as a pass-through so any legacy
 * import paths do not break at compile time.
 */
export { default } from "../steps/BasicInfoStep";