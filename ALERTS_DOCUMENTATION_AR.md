# نظام Alerts و Notifications - التحديثات

## الملفات التي تم إنشاؤها:

### 1. `src/components/Alert.tsx`
مكون Alert محترف يدعم:
- 4 أنواع من الـ Alerts: `success`, `error`, `warning`, `info`
- أيقونات مخصصة لكل نوع
- إغلاق تلقائي بعد 5 ثوانٍ
- زر إغلاق يدوي
- رسوم متحركة부드러운
- مكون `AlertContainer` لعرض عدة alerts في نفس الوقت

### 2. `src/utils/validation.ts`
مجموعة شاملة من دوال التحقق:

#### التحقق من البريد الإلكتروني:
- التحقق من عدم الترك فارغاً
- التحقق من صيغة البريد الإلكتروني الصحيحة
- رسائل خطأ واضحة بالعربية

#### التحقق من كلمة المرور:
- طول أدنى 8 أحرف
- يجب أن تحتوي على: حروف كبيرة وصغيرة وأرقام ورموز خاصة
- مؤشر قوة كلمة المرور: `weak` / `medium` / `strong`
- رسائل خطأ تفصيلية

#### التحقق من البيانات الأخرى:
- `validateFirstName()`: اسم أول صحيح
- `validateLastName()`: اسم أخير صحيح
- `validateLinkedInUrl()`: رابط LinkedIn صحيح
- `validateCVFile()`: فحص نوع الملف وحجمه
- `validateBio()`: السيرة الذاتية بطول معقول
- `validateSignupForm()`: التحقق الشامل من نموذج التسجيل
- `validateMentorProfileForm()`: التحقق من نموذج ملف المنتور

---

## التحديثات في الصفحات:

### `src/pages/auth/signup.tsx`
#### التحسينات:
1. ✅ إضافة مكون Alert للأخطاء
2. ✅ تحقق فوري من البريد الإلكتروني
3. ✅ مؤشر قوة كلمة المرور مع ألوان:
   - 🔴 **أحمر**: كلمة مرور ضعيفة
   - 🟡 **أصفر**: كلمة مرور متوسطة
   - 🟢 **أخضر**: كلمة مرور قوية
4. ✅ رسائل خطأ تحت كل حقل
5. ✅ تمسح الأخطاء عند تعديل الحقل

#### رسائل الخطأ:
```
"البريد الإلكتروني مطلوب"
"صيغة البريد الإلكتروني غير صحيحة. يجب أن تكون بصيغة: user@example.com"
"كلمة المرور مطلوبة"
"كلمة المرور يجب أن تكون 8 أحرف على الأقل"
"كلمة المرور يجب أن تحتوي على: حروف كبيرة وصغيرة وأرقام ورموز خاصة"
```

### `src/pages/auth/mentor-form.tsx`
#### التحسينات:
1. ✅ التحقق من صحة كل خطوة قبل الانتقال للخطوة التالية
2. ✅ مؤشرات خطأ تحت كل حقل
3. ✅ الـ Alert العام في الأعلى
4. ✅ التحقق من رابط LinkedIn في الوقت الفعلي
5. ✅ التحقق من ملفات CV (نوع وحجم)
6. ✅ التحقق من السيرة الذاتية

#### الخطوات والتحقق:
- **الخطوة 1**: سنوات الخبرة، رابط LinkedIn
- **الخطوة 2**: الصناعة، الخبرة ذات الصلة
- **الخطوة 3**: ملف CV، السيرة الذاتية
- **الخطوة 4**: التوفر، عدد المتعلمين

#### رسائل الخطأ:
```
"يرجى اختيار سنوات الخبرة"
"صيغة رابط LinkedIn غير صحيحة"
"يرجى اختيار الصناعة"
"يرجى اختيار خبرة واحدة على الأقل"
"يرجى رفع ملف CV واحد على الأقل"
"السيرة الذاتية يجب أن تكون 20 حرف على الأقل"
"السيرة الذاتية طويلة جداً (أقصى 1000 حرف)"
"حجم الملف يجب أن يكون أقل من 5MB"
"صيغ الملفات المسموحة: PDF, DOC, DOCX"
```

---

## المميزات:

### 1. التحقق الفوري
- التحقق الفوري من قوة كلمة المرور أثناء الكتابة
- التحقق من صحة رابط LinkedIn أثناء الكتابة
- رسائل خطأ فورية

### 2. تجربة المستخدم الجيدة
- ألوان مميزة للـ Alerts (أخضر للنجاح، أحمر للخطأ، إلخ)
- أيقونات واضحة
- رسائل بالعربية
- إغلاق تلقائي للـ Alerts

### 3. التحقق الشامل
- بيانات كاملة قبل الإرسال
- تحقق بخطوة بخطوة
- رسائل خطأ دقيقة وواضحة

### 4. سهولة الصيانة
- دوال التحقق مركزية في `validation.ts`
- معاد استخدام الكود
- سهل التوسع والتطوير

---

## الاستخدام:

### في أي صفحة جديدة:
```tsx
import { Alert } from "../../components/Alert"
import { validateEmail } from "../../utils/validation"

// في الـ component:
const [error, setError] = useState("")

// في template:
{error && <Alert type="error" message={error} dismissible />}

// في logic:
const emailValidation = validateEmail(email)
if (!emailValidation.valid) {
  setError(emailValidation.message)
}
```

